extends layout

block title
  title #{dictEntry.id} | Gedetailleerde info op Term Search
  meta(name='description', content='Voor de term met id #{dictEntry.id} is de volgende informatie gevonden op Term Search')

block breadcrumbs
  ul.breadcrumb
    li
      a(href="/") Home
    li.active
      | #{dictEntry.id}

block results
    .panel.panel-default
      .panel-body
        dl.dl-horizontal
          dt Domein(en)
          dd
            ul.list-unstyled
              each sf in dictEntry.subjectFields
                li
                  a(href='/duits-nederlands/vakgebied/#{sf.termUrl}') #{sf.termStr}
          dt Opmerkingen
          dd #{dictEntry.note}
          dt Vermelding
          dd 
            a(href='/duits-nederlands/#{dictEntry.deUrl}') #{dictEntry.de}
          br
          dt Bron
          dd
            a(href='http://iate.europa.eu/') IATE (= “Inter-Active Terminology for Europe”)

          
