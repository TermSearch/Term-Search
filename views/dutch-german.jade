extends layout

block title
  title Duits-Nederlandse vertalingen
  meta(name='description', content='')

block breadcrumbs
  ul.breadcrumb
    li
      a(href="/") Home
    li.active
      | Duits-Nederlands

block results
  h1 De eerste 1000 Duitse termen in de database                       
  p In de terminologische databank van de EU (IATE) zijn de volgende Duitse termen met een Nederlandse vertaling gevonden. Klik op de Duitse term om naar de Nederlandse vertaling te gaan.
    
  - alphabet = "ABCDEFGHIJKLMNOPQRSTUVWXYZ".split('')
  each letter in alphabet
    - var loopArray
    - loopArray = termArray.filter( function (term) { return term.termStr[0] === letter } )
    if (loopArray.length > 0)
      .panel.panel-default
        .panel-heading
          h1.panel-title
              | #{letter}                        
        .panel-body
            ul
              each term in loopArray
                li: a(href='/duits-nederlands/'+term.termUrl ) #{term.termStr}
